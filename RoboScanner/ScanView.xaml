<UserControl x:Class="RoboScanner.Views.ScanView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- заголовок -->
            <RowDefinition Height="Auto"/>
            <!-- кнопки -->
            <RowDefinition Height="Auto"/>
            <!-- полоса результатов -->
            <RowDefinition Height="*"/>
            <!-- две фотографии до низа -->
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <TextBlock Text="{DynamicResource Scan.Title}"
               FontSize="20" FontWeight="SemiBold" Margin="0,0,0,12"/>

        <!-- КНОПКИ -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,12">
            <Button x:Name="BtnStart" Width="140" Height="36" Margin="0,0,8,0"
              Click="BtnStart_Click" Content="{DynamicResource Scan.Btn.Start}"/>
            
            <Button x:Name="BtnScan"  Width="160" Height="36"
              Click="BtnScanOnce_Click" Content="{DynamicResource Scan.Btn.ScanOnce}"/>
        </StackPanel>

        <!-- ПОЛОСА РЕЗУЛЬТАТОВ (единая строка) -->
        <Border Grid.Row="2" Padding="10" CornerRadius="8"
            BorderBrush="#E5E7EB" BorderThickness="1" Background="#FBFBFB" Margin="0,0,0,12">
            <TextBlock x:Name="TxtResultLine" FontSize="25" />
        </Border>

        <!-- ДВЕ ФОТОГРАФИИ -->
        <Grid Grid.Row="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                
            </Grid.ColumnDefinitions>

            <!-- Камера 1 -->
            <Border BorderBrush="#E5E7EB" BorderThickness="1" CornerRadius="10" Padding="8">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <TextBlock Text="{DynamicResource Scan.Photo.Cam1}" Foreground="#6B7280" Margin="0,0,0,6"/>
                    <Grid Grid.Row="1">
                        <Image x:Name="ImgCam1" Stretch="Uniform" VerticalAlignment="Stretch"/>
                        <TextBlock x:Name="LblNoImg1" Text="{DynamicResource Scan.Photo.NoImage}"
                       Foreground="#9CA3AF" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Grid>
                </Grid>
            </Border>

            
        </Grid>
    </Grid>
</UserControl>
